# Tasks

- [x] Route `:app` module to `android-app`
- [x] Fix Android manifest and permissions for WebView + foreground service
- [x] Add minimal AppCompat theme resources
- [x] Add WebView console/error logging
- [ ] Run app smoke test on device/emulator
- [x] Add mobile layout overrides to prevent horizontal clipping
- [x] Enforce 44px touch targets for critical controls
- [x] Add `?debugLayout=1` HUD and layout checks
- [x] Fix escaped HTML attributes breaking layout
- [x] Remove global neon button CSS from mobile page
- [x] Wire SCAN to real engine endpoint (live-only) with data badge
- [x] Force asset sync from APK on launch to prevent stale dashboards
- [x] Add WebView cache hardening to avoid stale UI
- [x] Probe local server readiness before loading WebView
