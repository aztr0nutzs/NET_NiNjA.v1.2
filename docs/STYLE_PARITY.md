# Style Parity

Goal: replicate your reference HTML/CSS visuals using Compose tokens.

1) Put CSS in `design/style-reference.css`
2) Extract tokens -> `design/tokens.json`
3) Copy tokens into `app-ui/src/commonMain/resources/design/tokens.json`
4) Verify with the **Style Parity** tab.
